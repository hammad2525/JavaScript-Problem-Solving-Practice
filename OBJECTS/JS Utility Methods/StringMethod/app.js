// Use .length property to print length of a string cityName = ‘Tokyo’

let cityName = 'tokyo';

let a = cityName.length;

console.log(a);


// Find the index of ‘o’ character in the given string using indexOf method

let b = cityName.indexOf('o');
console.log(b);

// Use search method to locate ‘o’ in the given string and notice 
// the difference between indexOf and search methods

let s = cityName.search('o');
console.log(s);